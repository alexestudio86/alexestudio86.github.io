        <dialog id={service.id} >
            <header>
                <h2>{service.title}</h2>
                <button commandfor={service.id} command="close">&times;</button>
            </header>
            <div>
                { service.longDesc.map( (s) => {
                    s
                }) }
            </div>
        </dialog>